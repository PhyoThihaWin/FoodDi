<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/fullMenu"
        android:icon="@drawable/ic_filter_list"
        android:title="menu"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/all"
                android:actionLayout="@layout/full_screen_dialog"
                android:icon="@drawable/ic_letter_a"
                android:title="All"
                app:showAsAction="never" />
            <item
                android:id="@+id/food"
                android:actionLayout="@layout/full_screen_dialog"
                android:icon="@drawable/ic_dish"
                android:title="Food"
                app:showAsAction="never" />
            <item
                android:id="@+id/fruit"
                android:icon="@drawable/ic_apple"
                android:title="Fruit"
                app:showAsAction="never" />
            <item
                android:id="@+id/vegetable"
                android:icon="@drawable/ic_vegetable"
                android:title="Vegetable"
                app:showAsAction="never" />
            <item
                android:id="@+id/meat"
                android:icon="@drawable/ic_meat"
                android:title="Meat"
                app:showAsAction="never" />
            <item
                android:id="@+id/snack"
                android:icon="@drawable/ic_sandwich"
                android:title="Snack"
                app:showAsAction="never" />
        </menu>
    </item>
</menu>